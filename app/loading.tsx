// This loading UI is shown while the root route segment is waiting for data.

export default function RootLoading() {
  return (
    <>
      <h1>Loading content...</h1>
      <p>Please wait while the page data is being fetched.</p>
    </>
  );
}